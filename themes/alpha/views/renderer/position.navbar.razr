@foreach ($widgets as $widget)
    @if ($widget.type == 'widget.menu')

    @raw( $provider.render($widget, array_merge(['layout' => 'view://system/widgets/menu/navbar.razr'], $options)) )

    @else

    <ul class="uk-navbar-nav uk-visible-large">
        <li class="uk-parent" data-uk-dropdown>
            <a href="#">@( $widget.title )</a>
            <div class="uk-dropdown uk-dropdown-navbar">@raw( $provider.render($widget, $options) )</div>
        </li>
    </ul>
    @endif
@endforeach